[package]
name = "forja-wasm"
version = "0.1.0"
edition = "2021"
description = "Forja compiler core compiled to WebAssembly"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

forja = { path = "../.." }

[profile.release]
opt-level = "s"
lto = true
strip = true
